import React from "react";
import styled from "styled-components";

const Notification = () => <StyledText>Notification</StyledText>;

const StyledText = styled.Text`
  font-size: 40px;
  font-weight: bold;
`;

export default Notification;
